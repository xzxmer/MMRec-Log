# 让情绪学习到颜色、纹理

## 动机

既有情感编码器MLP仅实现维度提升，未编码色彩、艺术风格等视觉语义信息。论文Direct perception of affective valence from vision（Nature Communications）指出图像--->情绪，是可以忽略文本。论文并指出情绪来自图片低层特征，如颜色、纹理等。参考多模态情感分析领域，“图片特征 + 文本语义 = 更准确的完整情感理解”。

## 方法

采用蒸馏的方法，以 `(emotion, image)` 来监督情感编码器学习图片低层视觉特征。

## 架构（训练阶段）

- **视觉编码器（教师，冻结）**：输入图像 → 输出视觉特征 \( z_v \)

  使用基于ImageNet预训练的ResNet-50进行处理。为啥使用这个预训练的？

  1、浅层网络更关注“细节”，如边缘、颜色、纹理等基础视觉元素。
  2、中层网络关注“局部”，能将简单的边缘组合成更具结构的形状，如物体的部分（轮子、眼睛、鼻子）。
  3、深层网络关注“全局”，已经能识别出完整的、具有语义信息的概念，例如“猫”、“汽车”或“人脸”。

  z_v=model(image) ，其中z_v来自浅层网络。

- **情感编码器（学生，可训练）**：输入情感标签 → 输出情感编码 \( z_e \)

  z_e=MLP(emotion)

  loss1=1 - F.cosine_similarity(z_e, z_v, dim=-1).mean()

  loss2=  \(&alpha;\)* F.mse_loss(z_e, z_v)

  loss=loss1+loss2

**优化目标**：\( z_e &approx; z_v \)



![](C:\Users\陆飞宇\AppData\Roaming\Typora\typora-user-images\image-20260604110047072.png)

![image-20260604110158790](C:\Users\陆飞宇\AppData\Roaming\Typora\typora-user-images\image-20260604110158790.png)