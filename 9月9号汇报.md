UIES 模块整体
1、DenoiseGate：误点击 / 噪声过滤
让每个模态学一个 Sigmoid 门控值（0~1），对表征做逐元素缩放：如果某个模态在当前交互里是噪声（例如用户误点，或文本/图片与真实兴趣无关），门控值会趋近 0，从而“关掉”这部分信息。
id_emb    = id_emb * self.id_gate(id_emb)
text_emb  = text_emb * self.text_gate(text_emb)
image_emb = image_emb * self.image_gate(image_emb)

2、CrossModalFusion：跨模态融合
把三个模态在“模态维度”上堆成 3 个 token，即每个序列位置有 [id, text, image] 三个向量，用一个 self-attention 让三个模态互相看、互相融合，融合后对模态维取 mean，得到一个统一的物品表征 item_repr。
fusion_input = torch.stack([id_emb, text_emb, image_emb], dim=2)  # [B, L, 3, D]
fusion_input = fusion_input.view(B * L, 3, D)                    # [B*L, 3, D]
fused, _ = self.fusion(fusion_input, fusion_input, fusion_input) # self-attention
item_repr = fused.mean(dim=1).view(B, L, D)                      # [B, L, D]

3、InterestChangeNet：兴趣变化估计网络
相邻两个物品的表征变化 = prev_item → next_item盒时间间隔 delta_t（用 log(x+1) 压缩，避免间隔太大主导模型），拼接成 [prev_item, next_item, delta_t]，输入 MLP，输出一个兴趣变化标量。
prev_item = item_repr[:, :-1]  # [B, L-1, D]
next_item = item_repr[:, 1:]   # [B, L-1, D]

delta_t = timestamp[:, 1:] - timestamp[:, :-1]
delta_t = torch.log(delta_t.float() + 1)  # [B, L-1]

feature = torch.cat([prev_item, next_item, delta_t.unsqueeze(-1)], dim=-1)
delta_interest = F.softplus(self.change_mlp(feature)).squeeze(-1)  # [B, L-1]

4、LocalVelocity：局部速度 和 GlobalVelocity：注意力全局速度
1）delta_interest 本身就是每个相邻位置之间的局部兴趣变化率，直接作为 local_speed
2）对序列中每个“下一物品” next_item 打分，再用 softmax 归一化成注意力权重，加权求和得到整条序列的全局兴趣速度向量。
score = self.attention(next_item)          # [B, L-1, 1]
weight = F.softmax(score, dim=1)           # [B, L-1, 1]
global_speed = torch.sum(weight * next_item, dim=1)  # [B, D]

5、把局部 / 全局速度合成一个可用的 velocity
local_weight = F.softmax(local_speed, dim=1).unsqueeze(-1)   # [B, L-1, 1]
local_velocity = torch.sum(local_weight * next_item, dim=1)  # [B, D]
velocity = local_velocity + global_speed                     # [B, D]
